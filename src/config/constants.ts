import { typescriptFilePlugin } from "../plugins/ts-file";
import { Finalizer } from "../shared/models/finalizer.model";

export const DEFAULT_PLUGINS = [typescriptFilePlugin];

export const DEFAULT_FINALIZERS: Finalizer[] = [];
